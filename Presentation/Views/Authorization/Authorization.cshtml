@model Core.AuthorizationModel.LoginViewModel

@{
    if (User.Identity.IsAuthenticated)
    {
        Context.Response.Redirect("/Home/Index");
    }
    Layout = null;
}

<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Авторизация</title>
    <link href="https://fonts.googleapis.com/css2?family=Zen+Kaku+Gothic+Antique:wght@400;500;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="~/css/Authorization.css">
</head>
<body>
<div class="auth-container">
    <!-- Левая часть -->
    <div class="auth-form">
        <div class="logo">
            <img src="~/img/logo.svg" alt="Logo">
            <h1>Точка Сбора</h1>
        </div>

        <form asp-action="Authorization" method="post">
            <!-- Поле логина -->
            <div class="input-wrapper">
                <span class="input-label">LOGIN</span>
                <input asp-for="Login" type="text" placeholder="Введите логин" />
            </div>

            <!-- Поле пароля -->
            <div class="input-wrapper password-wrapper">
                <span class="input-label">PASSWORD</span>
                <input asp-for="Password" type="password" placeholder="••••••••••••••" />
                <img src="~/img/eye.svg" alt="Показать пароль" class="eye-icon">
            </div>

            <button class="login-btn" type="submit">
                <span class="login-text">Войти</span>
                <img src="~/img/chevron-right.svg" alt="chevron icon" class="chevron-icon">
            </button>
        </form>

        <!-- Текст "Нет аккаута?" -->
        <p class="centered-text">Нет аккаунта? <a asp-controller="Registration" asp-action="Register">SIGN UP</a></p>
    </div>

    <!-- Правая часть с паттерном -->
    <div class="auth-pattern">
        <img src="~/img/pattern.svg" alt="Pattern" />
    </div>
</div>
</body>
</html>